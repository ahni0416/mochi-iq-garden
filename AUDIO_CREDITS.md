# Audio credits

The first version uses short tones and ambient beds synthesized at runtime with the Web Audio API in `src/audio.js`. It contains no imported recordings, music, or third-party audio assets, so no external audio attribution is required.
